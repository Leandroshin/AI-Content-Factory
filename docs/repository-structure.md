# Estrutura Oficial do Repositório

Este documento registra a árvore inicial criada na Missão 000.

## Diretórios de Topo

- `docs/` — documentação do projeto
- `config/` — arquivos de configuração global
- `projects/` — configurações por projeto/nicho
- `core/` — infraestrutura central
- `shared/` — utilitários compartilhados
- `engines/` — módulos de domínio do pipeline
- `tests/` — testes do repositório
- `assets/` — recursos estáticos
- `output/` — artefatos gerados
- `temp/` — arquivos temporários
- `.github/` — automações e metadados do GitHub

## Engines Criadas

- `trend/`
- `research/`
- `script/`
- `narration/`
- `video/`
- `subtitle/`
- `thumbnail/`
- `publishing/`
- `analytics/`

## Regra Desta Missão

Nenhuma lógica de negócio foi implementada. Apenas a estrutura física do repositório foi criada.
